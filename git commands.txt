git checkout <branch>
git branch
git status
git add .
git commit -m <commit message>
git push orgin <branch>

Create a pull request on github
Get team member to approve pull request

# How to merge the master branch back to your individual branch:
git checkout master
git pull origin master
git checkout <branch>
git merge master